class barcos:


    #Cada jugador deberá colocar en su tablero:
    # 4 barcos tamaño 2x1.
    # 3 barcos tamaño 3x1.
    # 2 barcos tamaño 4x1.
    # 1 barco tamaño 5x1.
    #constructor y atributo de los barcos

    def __init__(self, tamaño, cantidad):
        
        self.tamaño= tamaño
        self.cantidad= cantidad
       

    # método para ubicar los barcos

    def pedir_ubicacion():


    def ubicar_barcos (self, filas, posicion, columnas):
        self.filas = filas
        self.posicion = posicion
        self.columnas = columnas
        for i in matriz_barco:
            filas = int(input("Inserte el número de la fila"))
            columnas = int(input("Inserte el número de la columna"))
            posicion = input("Inserte la ubicacion: 'h' para horizontal y 'v' para vertical")
            
barco_2 = tablero[1:2:,3:7] = "O"


        
    # Del 2do al 4to incluidos
print(my_list[1:4])


    def no_vivo(self):
        if self.salud <= 0:
            return True
        else:
            return False
#Inserte la posición del barco 1 (2x1). El usuario ha de escribir si
#  se quiere insertar en la 4a fila, en horizontal y ocupando las últimas 
# dos columnas: 4h9:10.
